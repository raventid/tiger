(menhir
 (modules parser)
 (flags ("--dump")))

(ocamllex
 (modules lexer))

(executable
 (name driver)
 (libraries core stdio core_unix.command_unix menhirLib))

(env (dev (flags (:standard -warn-error -A))))
